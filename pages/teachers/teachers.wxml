<view class="container">
 
    <view wx:for="{{  teacherList }}" class="icon-wrap" >
    <view data-wechat="{{ item.openid}}" bindtap="chooseTeacher">
         <image
          src="{{item.avatar}}"
          style="width:50px;height:50px;border-radius:5px"
        ></image>
      <view>{{item.name}}</view>
    </view>
      
    </view>
  
</view>
